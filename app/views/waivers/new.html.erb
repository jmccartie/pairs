<article>
	<h1>Release Form</h1>

	<p>
		I <span class="first_name"><%= @work_order.affected.contact.first_name %></span> <span class="last_name"><%= @work_order.affected.contact.last_name %></span> am the owner of the property at <span class="address"><%= @work_order.affected.contact.address.address %></span>
	</p>

	<%= render :partial => 'disclaimer' %>

	<h4>Special Instructions</h4>
	<div class="special_instructions"><%= @waiver.special_instructions %></div>
	<p>
		<%= Date.today %><br/>
		Signature
	</p>
</article>

<%= render 'form' %>