<article>
	<h1>Release Form</h1>

	<p>
		I <span class="name"><%= @work_order.affected.contact.name %></span> am the owner of the property at <span class="address"><%= @work_order.affected.contact.address.address %></span>
	</p>

	<%= render :partial => 'disclaimer' %>

	<h4>Special Instructions</h4>
	<p>
		<%= @waiver.special_instructions %><br/>
		<%= Date.today %><br/>
		Signature
	</p>
</article>

<%= render 'form' %>