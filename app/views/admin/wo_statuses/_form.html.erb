<%= simple_form_for [:admin, @wo_status] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
	<%= f.input :status %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
