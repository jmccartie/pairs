<%= simple_form_for [:admin, @zone] do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
	<%= f.input :zone %>
  </div>
  <div class="form-actions">
    <%= f.button :submit, :class=>"btn btn-primary" %>
		<%= link_to 'Cancel', admin_zones_path, :class=>"btn"  %>
  </div>
<% end %>
