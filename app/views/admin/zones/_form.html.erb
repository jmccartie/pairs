<%= simple_form_for [:admin, @zone] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
	<%= f.input :zone %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
